import Header from "../components/Header";
import Layout from "../components/Layout";

const EmailSetPage = () => {
  return (
    <Layout>
      <Header title={"이메일 설정"} />
    </Layout>
  );
};

export default EmailSetPage;
